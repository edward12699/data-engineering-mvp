version: 2

sources:
  - name: raw
    schema: main
    tables:
      - name: orders
        description: Raw orders ingested from CSV
      - name: customers
        description: Raw customers ingested from CSV
      - name: products
        description: Raw products ingested from CSV
